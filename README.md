# Exhale Compliance

A two-sided platform for physical-world compliance work: find businesses that
need inspections, audit them free, contract the remediation out to
subcontractors, and put them on a recurring agreement.

Three portals share one codebase:

| Portal | Who | What they do |
|---|---|---|
| **Employee** | Exhale staff | Sites, schedule, leads, quotes, contracts, vendor network |
| **Client** | Location owner | Compliance status, upcoming checks, service requests |
| **Vendor** | Subcontractor | Accept offered jobs, mark complete, track earnings |

## Running it

```bash
npm install
npm run dev
```

Opens on <http://localhost:5173>. No backend required to run it today — all
state is in-memory and resets on refresh. A Supabase schema is prepped in
`supabase/` for when that changes (see [Status](#status)).

## Project layout

```
src/
  App.jsx                  state + routing for all three portals
  theme.js                 design tokens (colours, shadows, fonts)
  data/
    catalog.js             DOMAINS, CHECKS, and INDUSTRIES — the master catalog
    status.js              lifecycle enums (quote, lead, contract, vendor)
    seed.js                demo data; replace with API calls
  lib/
    schedule.js            recurring due-date engine, groupByDomain()
    format.js              money, dates, margin math
    quotes.js              audit findings -> quote records
    email.js               subcontractor invite composer
    supabaseClient.js      Supabase client (reads VITE_SUPABASE_* env vars)
  components/
    ui.jsx                 Card, Stat, ScoreRing, IndustryBadge, DomainHeader...
    Nav.jsx                sidebar + mobile bottom nav w/ overflow sheet
    PortalPicker.jsx       role selection
    quotes.jsx             quote card, detail panel, bid review
  views/
    employee/              Dashboard, Sites, Leads, Contracts, Network
    client/                ClientPortal
    vendor/                VendorPortal, Marketplace (bid on open quotes)
supabase/
  schema.sql               Postgres schema + row-level security
  seed.sql                 demo data as inserts (generated by scripts/)
docs/
  SCHEMA.md                tables, endpoints, and what to build next
```

## Core flows

**Sales motion** — Leads board: a prospect moves New → Audit Scheduled →
Findings Ready → Proposal Sent → Signed. "Start free audit" opens a
step-by-step field checklist (pass/fail/na, note, photo) for every check that
industry requires. Failures become findings; findings become a proposal with
term selection and an on-site signature. Converting a signed lead creates the
site, the recurring contract, and the remediation quotes in one step.

**Margin** — every quote carries `clientPrice` and `vendorCost`. The spread is
the business, so it surfaces on the quote panel (live as you type), quote
cards, the Quotes page totals, vendor cards, and the dashboard.

**Recurring schedule** — each check has a cadence (Monthly → Annual). The
engine in `lib/schedule.js` derives the live due calendar that drives the
Schedule page, overdue counts, and the client's "what's coming up".

**Vendor network** — the Network page holds the checks catalog (which vendors
can do what, at what rate), the vendor roster, a recruiting CRM with a
ready-to-send invite email, and an in-house-vs-subcontractor break-even
calculator.

**Marketplace & bidding** — a quote with no vendor assigned posts automatically
to every vendor whose `checks` cover its category (matched by industry
specialty, not manual assignment). Vendors see it on their Marketplace tab and
submit a bid; the quote panel's "Bids received" section lets an employee award
the job to one bidder, which sets the vendor, cost, and status from that bid.

**Industry vs. compliance** — these are two independent axes, shown
separately everywhere: an `IndustryBadge` (with the real regulators) names
*who* the site is for, while checks are grouped by one of six `DOMAINS`
(Life Safety & Fire, Electrical & Power, HVAC & Environmental, Plumbing & Gas,
Structural & Envelope, Accessibility & Egress) describing *what* gets
inspected. A check's domain never changes based on which industry uses it.

## Demo data

Signing in with any portal uses seeded data:

- 9 sites across all 8 industry verticals (the client portal's two sites
  belong to the same demo client, "Downtown Property Group")
- $87,800 ARR across 9 recurring agreements
- Vendor portal is scoped to Redline Fire & Safety, seeded with an open
  offer, an accepted job, and a completed one
- Two open marketplace bids waiting on employee review

## Status

The frontend is feature-complete as a prototype, including multi-site
clients and a vendor bidding marketplace. What's still stubbed:

- **No persistence** — refresh resets everything. `supabase/schema.sql` has
  a full Postgres schema + row-level security ready to wire in; see
  `src/lib/supabaseClient.js` and `.env.example` for the client-side half.
- **No auth** — the portal picker is cosmetic; the Supabase schema includes
  a `profiles` table (role + client_id/vendor_id) for when this changes.
- **Invite email drafts but doesn't send**
- **Documents page is placeholder rows** — not wired to real storage yet
- **Audit photos are a boolean, not a file**

See [`docs/SCHEMA.md`](docs/SCHEMA.md) for the table design and API surface,
or `supabase/schema.sql` for the actual Postgres DDL these map to.
